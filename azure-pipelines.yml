# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:


- task: AzureCLI@2
  inputs:
    azureSubscription: 'NTT-Japan PaaS Developer(5)(6747898f-5136-4160-9d4c-848883672ac2)'
    scriptType: 'ps'
    scriptLocation: 'scriptPath'
    scriptPath: '.cmd'
